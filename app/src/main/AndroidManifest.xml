<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.endava.androidamweek">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.main.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.main.ScheduleActivity" />
        <activity android:name=".ui.speaker.SpeakerActivity" />
        <activity android:name=".ui.quizz.QuizzActivity" />
        <activity android:name=".ui.main.SignInActivity"/>
        <activity android:name=".ui.quizz.ContentQuizzActivity" />

        <service
            android:name=".ui.training.UpdateDataService"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest>